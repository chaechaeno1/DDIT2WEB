<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="board">

		<!-- 동적쿼리  ==> where조건이 동적이므로 where 자리에 넣음-->
		<sql id="dynamicCondition">
		   <where>
		      <if test="!(stype == '' or sword == '')">
		           ${stype} like  '%'||#{sword}||'%'
		       </if>
		   </where>
		</sql>
		
		
		<!-- 게시판 리스트 - 3개씩 조회 -->
		<select id="selectBoardList" parameterType="map" resultType="boardVO">
		 select A.* from (select rownum as rnum, B.*  from 
		 						(select * from boardtab  
		 							<include refid="dynamicCondition"/> 
		 						order by num desc) B 
		 				<![CDATA[ 
		 					where rownum <= #{end}) A  
		 				where A.rnum >=#{start} 
		 				]]>  

		</select>
		
		
		<!-- 게시판 글 갯수 구하기 - 조건에따라 수행(동적쿼리 사용) -->
		<select id="getTotalCont" parameterType="map" resultType="int">
			select count(*) from boardtab 
				<include refid="dynamicCondition"/> 
		
		
		</select>
		
		
		
		<!-- 삭제 //결과: int, param: 글번호(int), id이름=메소드이름 -->
		<delete id="deleteBoard" parameterType="int">
			delete from boardtap where num = #{data}
		</delete>
		
		
		<!-- 수정 //결과: int, param: vo, id이름=메소드이름 -->
		<update id="updateBoard" parameterType="boardVO">
			update boardtap set subject=#{subject}, mail=#{mail}, 
					content=#{content}, wip=#{wip}, wdate=sysdate
				where num=#{num}
		</update>
		
		<!-- 조회수 증가 //결과: int, param: 글번호(int), id이름=메소드이름 -->
		<update id="updateHit" parameterType="int">
			update boardtap set hit = hit+1
				where num = #{data}
		
		</update>
		
		
		<!-- 저장하기 // 결과: int, param: vo, id이름=메소드이름 -->
		<insert id="insertBoard" parameterType="int">
			insert into boardtap values()
 		
		</insert>
		



	
</mapper>	