<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="campsite">
	<select id="selectCampNo" resultType="campVo" parameterType="String">
		select * from campsite where camptype_code = #{camptype_code}
	</select>

</mapper>